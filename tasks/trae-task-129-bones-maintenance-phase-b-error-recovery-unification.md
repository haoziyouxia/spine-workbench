# Task 129 - bones 维护态 Phase B：错误提示与恢复动作统一

## 任务目标
在 128 通过后，统一 bones 区域的错误提示口径与恢复动作入口，让用户遇到失败时知道下一步怎么做。

## 本次范围
1. 仅处理 bones 内已有错误提示与恢复入口
2. 对齐错误信息语气、层级、按钮可见性
3. 强化“可恢复动作”优先级（重试/返回安全状态）

## 实现要求
1. 只做轻量收口，不扩展为新功能
2. 不新增业务状态机，不改核心数据流
3. 维持现有视觉体系，避免新增噪音

## 验收要求
- 常见错误提示口径统一（不冲突、不重复）
- 至少 2 条可恢复路径可被明确触达
- 不引入新 blocker
- 回写必须提供真实：
  - `git status --short`
  - `git log -1 --stat`
  - `app/` 下本章真实验证动作（若执行 build 则如实写）

## 回写补充
必须明确一句：相比 128，本章如何降低“发生错误后不知所措”的成本。

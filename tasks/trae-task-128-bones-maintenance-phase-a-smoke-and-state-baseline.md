# Task 128 - bones 维护态 Phase A：关键路径冒烟与可见状态基线

## 任务目标
在 bones 侧栏冻结后，建立维护态第一章基线：确认关键用户路径可走通、关键状态可见且不误导。

## 本次范围
1. 覆盖 bones 关键路径的轻量冒烟（进入、选择、查看、返回）
2. 统一关键状态提示的可见性与优先级（不做结构重构）
3. 仅限 bones 相关组件，不扩展到 ExportPreview 等其他模块

## 实现要求
1. 只做最小必要修正与收口，不新增复杂交互
2. 不推翻既有冻结结论，只补维护态基线缺口
3. 回写必须说明：用户进入 bones 后第一眼看到什么、第一步做什么

## 验收要求
- 关键路径可走通且无阻塞级问题
- 关键状态信息不冲突、不误导
- 改动范围可追溯且聚焦 bones
- 回写必须提供真实：
  - `git status --short`
  - `git log -1 --stat`
  - `app/` 下本章真实验证动作（若执行 build 则如实写）

## 回写补充
必须明确一句：相比冻结前，本章将哪一个维护风险降到了可接受水平。

# trae-task-118-bones-canvas-phase-b-interaction-polish-with-scenario-matrix

## 阶段目标
完成 bones 画布交互阶段B：按场景矩阵做交互收口，确保可验收与可量化。

## 范围
- `Canvas.tsx` / 相关样式

## 量化验收
1. `Current Task` 必须为 118。
2. 提供 S1-S6 场景矩阵（前置条件/步骤/预期结果）。
3. 覆盖：骨点拖拽、骨骼选择、多状态反馈、误操作恢复。
4. `npm run build` 再执行一次并记录结果。

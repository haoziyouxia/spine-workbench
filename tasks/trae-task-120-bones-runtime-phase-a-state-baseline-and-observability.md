# trae-task-120-bones-runtime-phase-a-state-baseline-and-observability

## 目标
建立 bones 运行态基线与可观测性，作为后续大批量任务验收底座。

## 量化验收
1. Current Task=120。
2. 输出 8 条运行态基线（加载/空状态/选中/切换/删除后回退等）。
3. `git status --short` 与 Files Changed 对齐。
4. `app/` 下执行 `npm run build` 并记录结果。
